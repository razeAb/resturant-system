# Backend base URL the driver app talks to.
# Use your machine's LAN IP (not localhost) when testing on a physical device
# with Expo Go, e.g. http://192.168.1.23:5001
EXPO_PUBLIC_API_URL=http://localhost:5001
